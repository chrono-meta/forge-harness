RISK: LOW
REASON: One shared code path through the platform module, all four targets improve, no cfg branches, thorough tests, matches maintainer's explicit ask.
QUOTE: "If you can get the same win through the platform module so that all four targets go through it, I will merge that the day it lands."
ACTION: Confirm the new lru dependency is acceptable by noting it's used unconditionally on the default verify path (per CONTRIBUTING's dependency rule), or preemptively justify it in the PR description.
